# Netlify redirect rules
# This file ensures proper routing for single-page app behavior

# Serve index.html for all routes (SPA mode)
# /* /index.html 200

# For static site, no redirect rules needed - files are served directly